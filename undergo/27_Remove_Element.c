
#include <stdio.h>
#include <stdlib.h>
#include <time.h>


int removeElement(int* nums, int numsSize, int val){

}

int main ()
{
    
    srand(time(NULL));
    int len = (rand() % 20) + 5;
    int *arr = (int*) malloc(sizeof(int)*len);
}
